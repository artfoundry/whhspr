Create new thread:
<%= render :partial => 'discussions/inline_form' %>
<p>
  Discussions:
</p>
<% @discussions.each do |discussion| %>
<%= link_to discussion.title, action: "show", id: discussion.id %><br>
<% end %>
